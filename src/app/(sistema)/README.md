# Sistema - Área Autenticada

Esta pasta conterá as rotas do sistema completo (área autenticada) quando for desenvolvido.

## Estrutura Futura

```
(sistema)/
├── dashboard/
├── leituras/
├── relatorios/
├── configuracoes/
└── ...
```

Todas as rotas aqui estarão disponíveis em `/sistema/*` (ex: `/sistema/dashboard`)
